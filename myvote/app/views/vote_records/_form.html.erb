<%= form_for(@vote_record) do |f| %>
  <% if @vote_record.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@vote_record.errors.count, "error") %> prohibited this vote_record from being saved:</h2>

      <ul>
      <% @vote_record.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :election_id %><br>
    <%= f.number_field :election_id %>
  </div>
  <div class="field">
    <%= f.label :method %><br>
    <%= f.text_field :method %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
